/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // basePath: '/Awatar9',
    images: {
        unoptimized: true,
    },
    trailingSlash: true,
};

module.exports = nextConfig;
